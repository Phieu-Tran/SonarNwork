interface:
  display_name: "Ship"
  short_description: "Build and validate release artifacts"
  default_prompt: "Use $ship to run final gates and produce validated release artifacts without publishing unless authorized."
